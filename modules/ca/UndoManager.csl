// Provides an undo manager for an object graph.
component UndoManager
{
	provides IUndoManager undoManager;
	provides IGraphObserver graphObserver;

	/*
		The graph this undo manager should track.
		This must be set once before the component is first used.
	 */
	receives IGraph graph;
};
