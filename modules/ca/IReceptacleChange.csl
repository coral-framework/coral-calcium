/*
	An object receptacle change event.
 */
interface IReceptacleChange
{
	// The changed object receptacle.
	readonly co.IPort receptacle;

	// Service previously bound to the receptacle (may be null).
	readonly co.IService previous;

	// Service currently bound to the receptacle (may be null).
	readonly co.IService current;
};
